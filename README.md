# [艾森豪矩陣任務優先權決策引擎]

## Proposal Report

### 動機與目標
在現代快節奏的生活與工作環境中，無論是面對多重報告的學生，還是處理繁雜專案的職場人士，常會面臨「任務超載」的困境。傳統的條列式待辦清單（To-Do List）僅能記錄事項，卻無法客觀衡量任務間的相對權重。這導致人們極易陷入「急迫性偏見（Urgency Bias）」——將大量精力耗費在眼前緊急卻不重要的瑣事上，反而一再推遲真正對長期目標與成長有益的關鍵任務。

目標為開發一個基於「艾森豪矩陣」理論的 CLI 終端機工具，透過量化「重要性」與「緊急性」兩個維度，自動為任務計算優先權分數，並利用高效的資料結構確保在任務繁多時仍能快速排定執行順序，協助使用者做出理性的時間管理決策。

### 預期功能
- 任務量化輸入： 使用者可新增任務，並為其「重要性」與「緊急性」進行評分（1-10 分）。
- 自動化優先權排序： 系統根據加權公式（例如：重要性占 40%，緊急性占 60%）自動計算權重，並即時維護一個「當前最應執行」的任務清單。
- 四象限視覺化輸出： 在終端機以 ASCII 圖表形式，將任務分類顯示在「重要且緊急」、「重要不緊急」、「不重要但緊急」、「不重要不緊急」四個區塊。
- 動態狀態管理： 支援更新任務狀態（完成、修改權重、刪除），並確保排序結果同步更新。
- 資料持久化： 支援將任務清單匯出與匯入為 CSV 或 JSON 格式，確保資料不會因程式關閉而遺失。

### 使用技術
- 程式語言： Python 。
- 開發環境： VS Code / Git (GitHub)。
- 外部套件： prettytable 或 tabulate (用於終端機表格美化)。

### 時程規劃
- Week 7 (Proposal)： 完成資料結構設計與核心權重演算法定義。
- Week 8-9： 實作基礎 CRUD（增刪查改）功能與核心排序模型。
- Week 10-11 (Prototype)： 完成核心 Heap 排序邏輯，達成能在終端機輸出排序後清單的雛形。
- Week 12-13： 實作四象限視覺化圖表輸出與檔案讀寫功能。
- Week 14： 進行壓力測試（模擬上百筆任務時的效能）與 Bug 修復。
- Week 15 (Final)： 錄製 Demo 影片並完成最終報告手稿。

### 與課程的關聯
1. Priority Queue / Max-Heap (優先權佇列 / 最大堆積)：  
   這是本專題的核心，我們不需要對所有任務進行全排序 $O(N \log N)$，我們只需要隨時知道「最高優先權」的任務是什麼。
   使用 Max-Heap 可以讓取出最高優先權任務的時間複雜度達到 $O(1)$，新增任務也僅需 $O(\log N)$。這在處理大量動態變動的任務時，效能遠優於一般的 Array。
3. Hash Table (雜湊表 / Python Dictionary)：  
   為了實現任務的快速查找，當使用者想要修改某個特定任務（例如：將「期末專題」標記為完成）時，透過 Hash Table 可以將任務名稱或 ID 直接對應到其在記憶體中的位址，達到 $O(1)$ 的查詢效率，避免線性搜尋。
4. Weighted Scoring Algorithm (加權評分演算法)：  
   透過這門課學到的邏輯思考，設計一個合理的公式來平衡「重要性」與「緊急性」。例如針對接近 Deadline 的任務給予非線性的權重加成。
6. 2D Array / List of Lists (二維陣列)：  
   在進行「四象限」視覺化分類輸出時，使用二維空間的概念來儲存各象限的任務清單，方便程式進行區域性的巡訪與列印。
---

## Prototype Report

### 目前進度

**核心邏輯（CLI，`src/app.py`）— 已完成**

| 功能 | 說明 |
|------|------|
| 任務資料模型 | `Task` 類別，包含重要度、緊急度、截止日期、建立時間 |
| 截止日期轉換 | Deadline 線性對應為緊急度（14 天以上 → 1 分，24 小時內 → 10 分）|
| 老化機制（Aging） | 任務每存活 1 天，綜合權重 +0.5，防止低優先任務永遠不被執行（Starvation） |
| 加權評分公式 | `score = 重要度 × 1.5 + 緊急度 + 老化加成` |
| MaxHeap | 實作插入 O(log N)、提取最大值 O(log N)、查看最大值 O(1)、依 ID 刪除 O(log N) |
| Hash Table 加速 | `pos_map`（`task_id → heap index`），使 `remove_by_id` 無需線性搜尋 |
| 全域重構 | `refresh()` 以 O(N) 重新建堆，修正因時間流逝造成的堆積性質破壞 |
| 資料持久化 | 任務狀態以 JSON 格式讀寫，程式重啟後可還原 |
| CLI 介面 | 支援 `add`、`next`、`done`、`list`、`save`、`load`、`exit` 指令 |

**Web UI（`src/web_app.py` + `src/templates/index.html`）— 已完成**

Proposal 中未規劃，因考量視覺化需求而額外開發：

- Flask REST API，將核心邏輯包裝為 JSON 介面
- 網頁介面：新增任務（支援手動緊急度或截止日期）、查看下一個任務、完成任務、刪除任務、儲存與載入
- **Heap 視覺化**：每次操作（Insert / Remove）後，逐步播放 Sift-Up / Sift-Down 的每一個比較與交換步驟
- **節點移動動畫**：交換時圓形節點沿著邊線實際滑移至新位置（CSS `transform: translate()` 動畫），並以顏色區分比較中（橘）與交換中（紅）的節點
- **底層陣列對照**：樹狀圖旁同步顯示 heap array 的實際排列，強化陣列與樹的概念對應
- 可關閉視覺化面板，讓一般使用者正常操作

---

### 遇到的困難

**1. Heap 性質因時間動態破壞**

`score` 包含隨時間增加的 Aging 加成與因 Deadline 接近而上升的緊急度。這導致 heap 在靜置一段時間後，父節點的分數不再保證大於子節點，違反堆積性質。

解法：設計 `refresh()` 方法，在每次 `list` 或 `next` 操作前以 O(N) heapify 重新建堆，確保取出的永遠是當下最高優先任務。

**2. 低優先任務的 Starvation**

重要度與緊急度都低的任務在純分數排序下可能永遠排不到，形成飢餓問題。

解法：加入 Aging 機制（每天 +0.5 分），使任何任務在積累足夠天數後都會自然浮上執行順位。

**3. Heap 視覺化的動畫實作**

最初以「每一步清空 SVG 重繪」的方式呈現步驟，節點瞬間跳至新位置，無法傳達「移動」的概念，視覺效果差。

解法：改用**持久化節點**策略——以 `nodeMap`（`taskId → <g> DOM 元素`）追蹤每個節點，更新位置時只修改 `transform: translate()`，由 CSS transition 自動補間動畫；交換時節點圓圈套用 `transform: scale(1.18)`，視覺上膨脹強調。新插入節點以兩層 `requestAnimationFrame` 延遲啟用 transition，避免初始渲染也觸發飛入動畫。

---

### 下一步計畫

1. **四象限視覺化**：依重要度與緊急度閾值，在畫面上以艾森豪矩陣四象限分類顯示任務，對應 Proposal 中的原始規劃
2. **任務編輯功能**：支援修改已存在任務的重要度或緊急度，並觸發 Heap 的 Sift-Up / Sift-Down 調整
3. **壓力測試**：模擬 100 筆以上任務，驗證 O(log N) 操作的實際效能表現
4. **Final Report 與 Demo 影片**：完整功能錄製展示，說明資料結構與演算法的關聯

---

### 與課程的關聯

**Max-Heap / Priority Queue**

本專題的核心。相較於每次都對整個陣列排序（O(N log N)），Max-Heap 讓「取得當前最高優先任務」維持 O(1)，插入新任務或刪除任意任務只需 O(log N)。Web UI 的視覺化直接呈現 Sift-Up（新節點上浮）與 Sift-Down（根節點替換後下沉）的過程，讓堆積的邏輯從抽象概念變成可互動的動畫。

**Hash Table**

`pos_map` 是一張 `task_id → heap array index` 的雜湊表。當使用者完成或刪除指定任務時，系統透過 `pos_map` 以 O(1) 定位該節點在陣列中的位置，避免 O(N) 的線性搜尋，使整體刪除操作維持 O(log N)。

**演算法設計（加權評分 + Aging）**

加權評分公式結合重要度、動態緊急度與老化機制，屬於課程中多項條件綜合排序的演算法設計實踐。Deadline 的線性轉換與 Starvation 的防治策略，也呼應課程中討論的優先權佇列實務問題。

---

## Final Report

### 專案說明
<!-- 完整描述你的專案做了什麼 -->

### 使用方式
<!-- 如何編譯、執行、使用你的程式 -->

### 與課程的關聯總結
<!-- 總結你的專題與進階程式設計及資料結構課程之間的關聯 -->
